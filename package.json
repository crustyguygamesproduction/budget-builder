{
  "name": "budget-builder",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --configLoader native",
    "build": "vite build --configLoader native",
    "check": "npm run lint && npm run test:money && npm run test:organiser && npm run test:security && npm run test:import && npm run build",
    "test:money": "node scripts/check-money-understanding.mjs",
    "test:organiser": "node scripts/check-money-organiser-intelligence.mjs",
    "test:security": "node scripts/check-security-validation.mjs",
    "test:import": "node scripts/check-import-analysis.mjs",
    "lint": "eslint .",
    "preview": "vite preview --configLoader native"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.104.1",
    "papaparse": "^5.5.3",
    "react": "^19.2.5",
    "react-dom": "^19.2.5"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.1",
    "eslint": "^10.2.1",
    "eslint-plugin-react-hooks": "^7.1.1",
    "eslint-plugin-react-refresh": "^0.5.2",
    "globals": "^17.5.0",
    "vite": "^8.0.10"
  }
}
